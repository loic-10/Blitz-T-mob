<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/drawer_layout"
    tools:context=".Views.Member.Activity.HomeMemberActivity">

    <FrameLayout
        android:id="@+id/fragment_container_member"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:layout_above="@+id/bottom_nav"/>

    <com.ismaeldivita.chipnavigation.ChipNavigationBar
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:layout_alignParentBottom="true"
        android:layout_centerInParent="true"
        android:background="@drawable/rounded_bottom_bar"
        android:elevation="8dp"
        android:padding="4dp"
        android:foregroundTint="@color/defaultColorWhite"
        app:cnb_menuResource="@menu/menu_bottom"
        app:cnb_badgeColor="@color/defaultColorWhite"
        app:cnb_unselectedColor="@color/defaultColorWhite"/>

</RelativeLayout>