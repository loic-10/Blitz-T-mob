<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blitz_t">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_b"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Views.Account.Activity.CreateAccountCustomerActivity"></activity>
        <activity android:name=".Views.Saving.Activity.SavingAccountActivity" />
        <activity android:name=".Views.Setting.Activity.SettingActivity" />
        <activity android:name=".Views.Transaction.Activity.DetailsTransactionActivity" />

        <service
            android:name=".Models.Notification.NotificationFirebase"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".Views.Transaction.Activity.MakeTransactionActivity" />
        <activity android:name=".Views.Transaction.Activity.TransactionAccountActivity" />
        <activity android:name=".Views.Account.Activity.AccountCustomerActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Views.Customer.Activity.HomeCustomerActivity" />
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".MapAgencyActivity" />
        <activity android:name=".Views.Customer.Activity.MembershipRequestCustomerActivity" />
        <activity android:name=".Views.Member.Activity.EditProfileMemberActivity" />
        <activity android:name=".Views.Country.CountryActivity" />
        <activity android:name=".Views.City.CityActivity" />
        <activity android:name=".Views.Member.Activity.LoginMemberActivity" />
        <activity android:name=".Views.Customer.Activity.LoginCustomerActivity" />
        <activity android:name=".Views.Member.Activity.RegisterMemberPart2Activity" />
        <activity android:name=".Views.Member.Activity.RegisterMemberPart1Activity" />
        <activity
            android:name=".IntroActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Views.Agency.Activity.DetailsAgencyActivity" />
        <activity android:name=".Views.Refund.Activity.RefundCreditAccountActivity" />
        <activity android:name=".Views.Credit.Activity.CreditAccountActivity" />
        <activity android:name=".Views.Agency.Activity.AgencyMicrofinanceActivity" />
        <activity android:name=".Views.Customer.Activity.HomeCustomerActivity" />
        <activity android:name=".Views.Member.Activity.HomeMemberActivity" />
        <activity android:name=".Views.Account.Fragment.AccountCustomerFragment" />
        <activity android:name=".Views.Microfinance.Fragment.OthersMicrofinancesFragment" />
        <activity
            android:name=".ChoiceLogActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>